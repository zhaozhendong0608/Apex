# 全局 AI 行为约束与指令路由总纲 (Vibe Coding 3.0 Standard)

## 1. 核心路由指令 (User Command Router & Super Shortcuts)
当用户输入以下极简数字/符号或对应关键词时，请**静默读取**对应的 SOP 规约文件并严密执行：

### 🤖 预设引导菜单 (当用户输入 "?" / "help" 或开启新会话时自动提示)
- **输入 [0] 或 "老项目"** ➔ 破局老项目：自动扫描目录树，生成 `.ai/legacy_arch.md` 索引地图。
- **输入 [1] 或 "规划"** ➔ 新需求规划：自动开启 Grill-Me 选择题对撞，拆解 Task 写入 `.ai/status.md`。
- **输入 [1+] 模块名** ➔ 新大模块开发：自动在 `docs/modules/` 创建 PRD，并拆解 Task 写入看板。
- **输入 [2] 或 "编码"** ➔ 自动执行 `board.py start` 锁定唯一 `ACTIVE` 任务，看门狗护栏代码编写。
- **输入 [3] 或 "排错"**（可直接粘贴报错+发 3） ➔ 小黄鸭微创修复，禁止伪造保底假数据。
- **输入 [4] 或 "验收"** ➔ 自动执行 `board.py complete` 标记 DONE，自动同步更新 `docs/` 文档。
- **输入 [5] 或 "归档"** ➔ 压缩对话 Changelog，追加写入 `.ai/handover.md` 长效黑匣子。
- **输入 [6] 或 "恢复"** ➔ 一键断点复活：读取 `.ai/status.md` 和 `.ai/handover.md` 恢复记忆。


## 2. 老项目接入与归档法则 (Legacy Project Protocol)
若本项目为老项目/现有代码库，初次对话必须静默遵循“巨型老项目破局四步法”：
- **鸟瞰全局**：优先调阅根目录树（前3层）与包依赖文件（如 package.json, pom.xml, go.mod, requirements.txt），禁止直接漫无目的地读取大量业务源码。
- **建立索引与归档**：识别老项目核心模块，梳理关系并自动更新 `docs/02-architecture.md` 及 `.ai/legacy_arch.md`。
- **单链路切片**：针对具体修改或新需求，仅精准抓取关联路径上的 3~4 个文件。
- **逆向考古**：对无注释或复杂老函数，先逆向补全中文注释与探针断言单测，锁定老代码行为后再实施变更。

## 3. 状态维护铁律 (State Machine Rules)
- 任何关于任务状态（TODO / IN_PROGRESS / DONE）的变更，**必须且仅能**通过运行 Python 脚本 `.ai/scripts/board.py` 完成！
- **严禁 AI 直接手写修改 `.ai/status.md`！**
- 保持单焦点原则：全局同一时刻只允许一个任务处于 `IN_PROGRESS` 状态。

## 4. 语言与日志规范 (Language & Logging Rules)
- 与用户的每一次对话、解释、报告必须使用**中文（简体）**。
- 所有代码注释、文档、生成的日志提示等用户可见文本必须使用**中文**。
- **日志滥用控制与联调日志**：严禁在代码中随意添加大量无意义调试日志。仅在关键业务节点（特别是**调用第三方接口/外部服务**）时，**必须追加必要的联调日志（清晰记录入参和返参）**，方便联调排查。
- **数据真实性原则**：**严禁自主补充保底测试数据/兜底硬编码 Mock 数据**，接口或数据缺失时应如实暴露异常或报错，不得用假数据掩盖问题。

## 5. 文档同步护栏 (Documentation Guardrails)
- 每次代码涉及数据库结构、API 路由或系统架构变动时，必须在执行 [4] 验收或 [5] 归档时自动同步更新 `docs/` 下的对应文档。

