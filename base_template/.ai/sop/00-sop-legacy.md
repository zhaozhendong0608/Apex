# 🏛️ SOP: 老项目破局与切片索引 (00-sop-legacy)

## 📌 核心目标
专门针对老项目/现有代码库，进行顶层架构扫描、黑盒线索解密（路由/表名/探查菜单），防范大模型上下文超载，静默建立与更新 `.ai/legacy_arch.md` 切片地图。

---

## 🛡️ 阶段一：Legacy Guardrail (老项目破局护栏)
在扫描或分析老代码前，AI 必须**强制遵循以下 3 条防溢出原则**：

1. **防上下文溢出原则**：严禁一次性全量读取/扫描深层业务源代码文件。
2. **前 3 层骨架原则**：初次鸟瞰仅读取前 3 层目录结构与包依赖文件（如 `package.json`/`pom.xml`/`go.mod`）。
3. **切片留痕原则**：分析得出的老代码模块链路必须静默更新写入 `.ai/legacy_arch.md`。

---

## 🤖 阶段二：AI 执行流程 (支持 3 大触发模式)

### 模式 A：顶层骨架鸟瞰 (输入 `0` 或 `0 骨架`)
1. **静默调用 Python 确切脚本**获取真实前 3 层目录树与依赖：
   ```bash
   python3 .ai/scripts/arch.py tree 3
   python3 .ai/scripts/arch.py deps
   ```
2. 识别项目框架（MVC、微服务、前端 SPA）、入口文件与核心结构。
3. 自动生成或更新 `.ai/legacy_arch.md` 顶层索引。

### 模式 B：黑盒线索解密 (输入 `0 接口URL` / `0 关键词` / `0 表名`)
1. 根据用户给出的线索（如 `/api/refund` 或表名 `t_order`），在全盘路由表或注解中执行全局搜索。
2. 逆向定位到对应的 3~4 个核心 Controller/Service/DAO 文件。
3. **静默调用 Python 脚本追加结构化切片**：
   ```bash
   python3 .ai/scripts/arch.py append "退款模块" "src/controllers/refund.js" "处理订单退款与流水计算"
   ```


### 模式 C：探查菜单生成 (输入 `0 菜单`)
1. 扫描前 2 层主要文件夹。
2. 为用户整理出 3~5 个主要业务模块卡片，以 A/B/C 选择题形式抛给用户确认方向。

---

## 💬 阶段三：反哺反馈 (给用户的回复)

扫描或解密完成后，仅生成以下极简破局卡片：

---
🏛️ **老项目架构索引 (.ai/legacy_arch.md) 已更新！**

#### 🔍 本次识别到的老代码资产：
- **项目框架**：[如：Java Spring Boot + MyBatis]
- **定位模块/链路**：[如：`src/controllers/refund/` 退款服务]
- **风险提醒**：[如：需注意修改该接口影响下游报表统计]

---
👉 **老项目切片地图已落盘至 `.ai/legacy_arch.md`！**
👉 **请回复 [1] 发起需求规划，或回复 [2] 开启特定 Task 的编写！**
